-- 1: a
-- 2: loop: (a \/ ~a)

MODULE main
 VAR
    a: boolean;
 ASSIGN
    -- Initial Conditions
    init(a):= TRUE;
    -- init(PC):= 1; -- program counter starts at 1.

    -- Transition Relations
    next(a) :=
      case
        a: {TRUE, FALSE};
        TRUE: {TRUE, FALSE};
      esac;
