-- curr: a
-- next: (a -> ~a') /\ (~a -> a)
MODULE main
 VAR
    a: boolean;
 ASSIGN
    -- Initial Conditions
    init(a):= TRUE;

    -- Transition Relations
    next(a) :=
      case
        (a): {FALSE, TRUE};
        TRUE: a;
      esac;
