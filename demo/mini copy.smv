MODULE main
 VAR
    a: boolean;
 ASSIGN
    -- Initial Conditions
    init(a):= FALSE;

    -- Transition Relations
    next(a) :=
      case
        a: {FALSE};
        !a: {TRUE};
        TRUE: a;
      esac;

