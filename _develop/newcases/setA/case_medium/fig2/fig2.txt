[program]

void main(int x) {
    int y;

    observe;

    if * {
        @ y = 2 * x;

        while (y > 0) {
            y = y - 1;
            x = 2 * x;
        }
    } else {
        y = x;

        while (y > 0) {
            y = y - 1;
            x = 4 * x;
        }
    }

    observe;
}


[formula]
forall pi. forall pii.  {x_pi = x_pii} -> X {x_pi = x_pii}

[depth]
15

[outputs]
