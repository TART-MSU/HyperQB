[program]

void main(int x, int n) {
    int c;

    observe;

    if * {
        c = 0;

        while (x < n) {
            x = x + 1;
            c = c + 1;
        }

    } else {
        @ c = 1;

        while (x < n) {
            x = x + 1;
            c = c + 1;
        }
    }

    observe;
}

[formula]
forall pi. forall pii.  {n_pi = n_pii & x_pi = x_pii} -> X {c_pi = c_pii}

[depth]
20


[outputs]