[program]
void main(int l, int h) {
    int res;
    int z;

    observe;

    res = 0;

    if (h == 0) {
        res = -1;
    } else {
        z = l + h;
    }

    @ res = res;

    observe;
}

[formula]
forall pi. forall pii.  {l_pi = l_pii} -> X {res_pi = res_pii}

forall A. forall B. G((*l[A] = l[B]*) -> (*res[A] = res[B]*))

[outputs]
res

