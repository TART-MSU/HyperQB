MODULE main
 VAR
    a: boolean;
    b: boolean;
 ASSIGN
    -- Initial Conditions
    
    init(a):= FALSE;
    init(b):= TRUE;
    -- Transition Relations
    next(a) := 
      case
        TRUE: {TRUE, FALSE};
      esac;
    next(b) :=
      case
        TRUE: b;
        (a): !b;
      esac;
