[program]

void main(int x, int n) {
    int t;

    observe;

    if * {
        while (x < n) {
            x = x + x;
        }
    } else {
        while (x < n) {
            @ t = x;
            x = x + t;
        }
    }

    observe;
}


[formula]
forall pi. forall pii.  {x_pi = x_pii & n_pi = n_pii} -> X {x_pi = x_pii}

[depth]
10

[outputs]
